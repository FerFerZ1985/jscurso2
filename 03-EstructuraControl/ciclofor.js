// Ciclo for - Repite un bloque de codigo mientras se cumpla una condicion
    // 1 Desde donde comienza el ciclo
    // 2 condicion del ciclo
    // 3 indicar si incrementa o decrece


// for(let numero = 1; numero <= 20; numero++){
//     console.log(numero);
// }

// Recorrer arreglo

const frutas = ['Cereza', 'Manzana', 'Naranja', 'Limon', 'Melon', 'Kiwi', 'Banana', 'Kiwi'];
for(let i = 0; i <frutas.length; i++){
    console.log(frutas[i]);
}